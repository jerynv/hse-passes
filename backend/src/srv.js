import wss from "./wss.js";
import server from "./http.js";

server;
wss;
// This is the main entry point for the server. It imports the HTTP and WebSocket servers and starts them.
// The HTTP server handles RESTful API requests, while the WebSocket server manages real-time communication.
// The server is set up to listen on the specified ports and handle incoming connections.
// The WebSocket server is set up to handle connections, messages, and errors.

